# where test, train folders and 1 CSV file will be placed

1) inside this two folders.. each image has 1 .xml file with same name.
2) in id. csv, should give ids for the classes.
 
folder structure:
dataset:
   ->train:
        ->111.jpg
        ->111.xml
        ->222.jpg
        ->222.xml
        ...
        ...
   ->test:
        ->1111.jpg
        ->1111.xml
        ->2222.jpg
        ->2222.xml
        ...
        ...
   ->id.csv:
